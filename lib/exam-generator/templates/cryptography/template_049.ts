import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate049: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (49/80): which statement is most accurate about cryptographic hash properties regarding protocol negotiation hazards?",
  "variables": {},
  "correct": "Well-scoped designs use cryptographic hash properties deliberately to meet protocol negotiation hazards without confusing hashing with encryption.",
  "distractors": [
    "It means MACs provide confidentiality without encryption.",
    "It eliminates nonce requirements for all modes.",
    "It implies RSA keys never need rotation."
  ]
}
