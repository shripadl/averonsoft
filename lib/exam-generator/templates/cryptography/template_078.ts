import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate078: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (78/80): which statement is most accurate about side-channel awareness (introductory) regarding deployment and configuration hazards?",
  "variables": {},
  "correct": "Well-scoped designs use side-channel awareness (introductory) deliberately to meet deployment and configuration hazards without confusing hashing with encryption.",
  "distractors": [
    "It guarantees ciphertext length reveals no information.",
    "It removes the distinction between encryption and encoding.",
    "It implies MAC tags should be truncated to one byte always."
  ]
}
