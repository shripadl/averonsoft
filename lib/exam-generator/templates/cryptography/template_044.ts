import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate044: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (44/80): which statement is most accurate about downgrade resistance concepts regarding key lifecycle hygiene?",
  "variables": {},
  "correct": "Well-scoped designs use downgrade resistance concepts deliberately to meet key lifecycle hygiene without confusing hashing with encryption.",
  "distractors": [
    "It guarantees asymmetric encryption is always faster than symmetric.",
    "It removes the need to verify certificate chains.",
    "It implies HMAC can replace encryption for secrecy."
  ]
}
