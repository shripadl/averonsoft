import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate036: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (36/80): which statement is most accurate about RSA-style public-key ideas (introductory) regarding key lifecycle hygiene?",
  "variables": {},
  "correct": "Well-scoped designs use RSA-style public-key ideas (introductory) deliberately to meet key lifecycle hygiene without confusing hashing with encryption.",
  "distractors": [
    "It guarantees asymmetric encryption is always faster than symmetric.",
    "It removes the need to verify certificate chains.",
    "It implies HMAC can replace encryption for secrecy."
  ]
}
