import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate076: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (76/80): which statement is most accurate about downgrade resistance concepts regarding deployment and configuration hazards?",
  "variables": {},
  "correct": "Well-scoped designs use downgrade resistance concepts deliberately to meet deployment and configuration hazards without confusing hashing with encryption.",
  "distractors": [
    "It guarantees asymmetric encryption is always faster than symmetric.",
    "It removes the need to verify certificate chains.",
    "It implies HMAC can replace encryption for secrecy."
  ]
}
