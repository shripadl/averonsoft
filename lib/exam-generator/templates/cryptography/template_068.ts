import type { TemplateConfig } from '../../template-config'

export const cryptographyTemplate068: TemplateConfig = {
  "domain": "cryptography",
  "stem": "Cryptography study (68/80): which statement is most accurate about RSA-style public-key ideas (introductory) regarding deployment and configuration hazards?",
  "variables": {},
  "correct": "Well-scoped designs use RSA-style public-key ideas (introductory) deliberately to meet deployment and configuration hazards without confusing hashing with encryption.",
  "distractors": [
    "It guarantees asymmetric encryption is always faster than symmetric.",
    "It removes the need to verify certificate chains.",
    "It implies HMAC can replace encryption for secrecy."
  ]
}
