import type { TemplateConfig } from '../../template-config'

export const databasesTemplate052: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (52/80): which answer best reflects normalization and redundancy control in relation to schema evolution?",
  "variables": {},
  "correct": "Practitioners should connect normalization and redundancy control to concrete schema evolution outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
