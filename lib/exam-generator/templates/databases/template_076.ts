import type { TemplateConfig } from '../../template-config'

export const databasesTemplate076: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (76/80): which answer best reflects vacuum and bloat concepts (generic) in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect vacuum and bloat concepts (generic) to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
