import type { TemplateConfig } from '../../template-config'

export const databasesTemplate037: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (37/80): which answer best reflects isolation levels (introductory) in relation to operational risk?",
  "variables": {},
  "correct": "Practitioners should connect isolation levels (introductory) to concrete operational risk outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees ORMs always emit optimal SQL.",
    "It removes the value of explain plans for tuning.",
    "It implies foreign keys are only for documentation."
  ]
}
