import type { TemplateConfig } from '../../template-config'

export const databasesTemplate014: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (14/80): which answer best reflects OLTP versus OLAP workloads (introductory) in relation to consistency?",
  "variables": {},
  "correct": "Practitioners should connect OLTP versus OLAP workloads (introductory) to concrete consistency outcomes when designing schemas and operations.",
  "distractors": [
    "It means backups never need restore drills.",
    "It eliminates the role of connection limits.",
    "It implies vacuum operations never affect latency."
  ]
}
