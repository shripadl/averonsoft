import type { TemplateConfig } from '../../template-config'

export const databasesTemplate069: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (69/80): which answer best reflects isolation levels (introductory) in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect isolation levels (introductory) to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees ORMs always emit optimal SQL.",
    "It removes the value of explain plans for tuning.",
    "It implies foreign keys are only for documentation."
  ]
}
