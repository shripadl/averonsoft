import type { TemplateConfig } from '../../template-config'

export const databasesTemplate028: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (28/80): which answer best reflects vacuum and bloat concepts (generic) in relation to performance?",
  "variables": {},
  "correct": "Practitioners should connect vacuum and bloat concepts (generic) to concrete performance outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
