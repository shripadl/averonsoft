import type { TemplateConfig } from '../../template-config'

export const databasesTemplate056: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (56/80): which answer best reflects query plans and cost-based optimization (introductory) in relation to schema evolution?",
  "variables": {},
  "correct": "Practitioners should connect query plans and cost-based optimization (introductory) to concrete schema evolution outcomes when designing schemas and operations.",
  "distractors": [
    "It means every table must have exactly one column.",
    "It removes the need for primary keys in relational models.",
    "It implies indexes always slow down reads."
  ]
}
