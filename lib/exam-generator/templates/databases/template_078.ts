import type { TemplateConfig } from '../../template-config'

export const databasesTemplate078: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (78/80): which answer best reflects OLTP versus OLAP workloads (introductory) in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect OLTP versus OLAP workloads (introductory) to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It means backups never need restore drills.",
    "It eliminates the role of connection limits.",
    "It implies vacuum operations never affect latency."
  ]
}
