import type { TemplateConfig } from '../../template-config'

export const databasesTemplate029: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (29/80): which answer best reflects partitioning for large tables (introductory) in relation to performance?",
  "variables": {},
  "correct": "Practitioners should connect partitioning for large tables (introductory) to concrete performance outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees ORMs always emit optimal SQL.",
    "It removes the value of explain plans for tuning.",
    "It implies foreign keys are only for documentation."
  ]
}
