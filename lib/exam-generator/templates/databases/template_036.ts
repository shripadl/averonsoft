import type { TemplateConfig } from '../../template-config'

export const databasesTemplate036: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (36/80): which answer best reflects normalization and redundancy control in relation to operational risk?",
  "variables": {},
  "correct": "Practitioners should connect normalization and redundancy control to concrete operational risk outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
