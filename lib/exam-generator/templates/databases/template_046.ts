import type { TemplateConfig } from '../../template-config'

export const databasesTemplate046: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (46/80): which answer best reflects OLTP versus OLAP workloads (introductory) in relation to operational risk?",
  "variables": {},
  "correct": "Practitioners should connect OLTP versus OLAP workloads (introductory) to concrete operational risk outcomes when designing schemas and operations.",
  "distractors": [
    "It means backups never need restore drills.",
    "It eliminates the role of connection limits.",
    "It implies vacuum operations never affect latency."
  ]
}
