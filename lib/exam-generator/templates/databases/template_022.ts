import type { TemplateConfig } from '../../template-config'

export const databasesTemplate022: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (22/80): which answer best reflects pessimistic versus optimistic concurrency in relation to performance?",
  "variables": {},
  "correct": "Practitioners should connect pessimistic versus optimistic concurrency to concrete performance outcomes when designing schemas and operations.",
  "distractors": [
    "It means backups never need restore drills.",
    "It eliminates the role of connection limits.",
    "It implies vacuum operations never affect latency."
  ]
}
