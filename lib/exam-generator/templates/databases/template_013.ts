import type { TemplateConfig } from '../../template-config'

export const databasesTemplate013: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (13/80): which answer best reflects partitioning for large tables (introductory) in relation to consistency?",
  "variables": {},
  "correct": "Practitioners should connect partitioning for large tables (introductory) to concrete consistency outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees ORMs always emit optimal SQL.",
    "It removes the value of explain plans for tuning.",
    "It implies foreign keys are only for documentation."
  ]
}
