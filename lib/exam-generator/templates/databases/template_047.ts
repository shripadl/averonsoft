import type { TemplateConfig } from '../../template-config'

export const databasesTemplate047: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (47/80): which answer best reflects backup versus restore testing in relation to operational risk?",
  "variables": {},
  "correct": "Practitioners should connect backup versus restore testing to concrete operational risk outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees partial indexes are always larger than full indexes.",
    "It removes the benefit of read replicas for scaling reads.",
    "It implies transactions cannot span multiple statements."
  ]
}
