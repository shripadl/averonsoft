import type { TemplateConfig } from '../../template-config'

export const databasesTemplate068: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (68/80): which answer best reflects normalization and redundancy control in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect normalization and redundancy control to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
