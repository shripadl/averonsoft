import type { TemplateConfig } from '../../template-config'

export const databasesTemplate077: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (77/80): which answer best reflects partitioning for large tables (introductory) in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect partitioning for large tables (introductory) to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees ORMs always emit optimal SQL.",
    "It removes the value of explain plans for tuning.",
    "It implies foreign keys are only for documentation."
  ]
}
