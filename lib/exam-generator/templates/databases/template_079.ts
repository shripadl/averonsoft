import type { TemplateConfig } from '../../template-config'

export const databasesTemplate079: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (79/80): which answer best reflects backup versus restore testing in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect backup versus restore testing to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees partial indexes are always larger than full indexes.",
    "It removes the benefit of read replicas for scaling reads.",
    "It implies transactions cannot span multiple statements."
  ]
}
