import type { TemplateConfig } from '../../template-config'

export const databasesTemplate004: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (4/80): which answer best reflects normalization and redundancy control in relation to consistency?",
  "variables": {},
  "correct": "Practitioners should connect normalization and redundancy control to concrete consistency outcomes when designing schemas and operations.",
  "distractors": [
    "It means isolation levels are purely cosmetic settings.",
    "It removes the benefit of covering indexes.",
    "It implies ACID properties apply only to NoSQL stores."
  ]
}
