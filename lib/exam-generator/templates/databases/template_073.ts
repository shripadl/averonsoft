import type { TemplateConfig } from '../../template-config'

export const databasesTemplate073: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (73/80): which answer best reflects materialized views tradeoffs in relation to observability of workloads?",
  "variables": {},
  "correct": "Practitioners should connect materialized views tradeoffs to concrete observability of workloads outcomes when designing schemas and operations.",
  "distractors": [
    "It guarantees serializable isolation without any locking.",
    "It eliminates the value of foreign key constraints.",
    "It implies normalization always increases redundancy."
  ]
}
