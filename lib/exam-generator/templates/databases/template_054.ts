import type { TemplateConfig } from '../../template-config'

export const databasesTemplate054: TemplateConfig = {
  "domain": "databases",
  "stem": "Database concepts (54/80): which answer best reflects pessimistic versus optimistic concurrency in relation to schema evolution?",
  "variables": {},
  "correct": "Practitioners should connect pessimistic versus optimistic concurrency to concrete schema evolution outcomes when designing schemas and operations.",
  "distractors": [
    "It means backups never need restore drills.",
    "It eliminates the role of connection limits.",
    "It implies vacuum operations never affect latency."
  ]
}
