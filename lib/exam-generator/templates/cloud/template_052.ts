import type { TemplateConfig } from '../../template-config'

export const cloudTemplate052: TemplateConfig = {
  "domain": "cloud",
  "stem": "For generic public-cloud study (52/80): which statement aligns best with “VPC-style isolated virtual networks” when discussing operational monitoring expectations?",
  "variables": {},
  "correct": "Policies, monitoring, and architecture choices around VPC-style isolated virtual networks should explicitly address operational monitoring expectations under a shared-responsibility model.",
  "distractors": [
    "It means object storage cannot version objects.",
    "It implies compute instances never need patching.",
    "It removes the concept of availability zones."
  ]
}
