import type { TemplateConfig } from '../../template-config'

export const cloudTemplate057: TemplateConfig = {
  "domain": "cloud",
  "stem": "For generic public-cloud study (57/80): which statement aligns best with “object lifecycle and storage classes” when discussing operational monitoring expectations?",
  "variables": {},
  "correct": "Policies, monitoring, and architecture choices around object lifecycle and storage classes should explicitly address operational monitoring expectations under a shared-responsibility model.",
  "distractors": [
    "It replaces the need for backup copies entirely.",
    "It mandates identical hardware in every rack.",
    "It eliminates monitoring because the provider observes everything."
  ]
}
