import type { TemplateConfig } from '../../template-config'

export const cloudTemplate028: TemplateConfig = {
  "domain": "cloud",
  "stem": "For generic public-cloud study (28/80): which statement aligns best with “bucket policies versus identity policies” when discussing cost drivers for egress and requests?",
  "variables": {},
  "correct": "Policies, monitoring, and architecture choices around bucket policies versus identity policies should explicitly address cost drivers for egress and requests under a shared-responsibility model.",
  "distractors": [
    "It means object storage cannot version objects.",
    "It implies compute instances never need patching.",
    "It removes the concept of availability zones."
  ]
}
