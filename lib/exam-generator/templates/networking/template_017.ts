import type { TemplateConfig } from '../../template-config'

export const networkingTemplate017: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (17/80): which option best fits “OSI layering vocabulary” in the context of header fields versus payload?",
  "variables": {},
  "correct": "Understanding OSI layering vocabulary helps place controls and performance decisions correctly when analyzing header fields versus payload.",
  "distractors": [
    "It means routers learn hostnames instead of prefixes.",
    "It eliminates the need for MTU discovery.",
    "It requires identical MAC addresses on all hosts."
  ]
}
