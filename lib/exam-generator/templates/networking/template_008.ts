import type { TemplateConfig } from '../../template-config'

export const networkingTemplate008: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (8/80): which option best fits “stateful versus stateless firewalls” in the context of failure domains?",
  "variables": {},
  "correct": "Understanding stateful versus stateless firewalls helps place controls and performance decisions correctly when analyzing failure domains.",
  "distractors": [
    "It guarantees infinite bandwidth without congestion.",
    "It removes the need for any link-layer addressing.",
    "It implies DNS always encrypts application payloads."
  ]
}
