import type { TemplateConfig } from '../../template-config'

export const networkingTemplate078: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (78/80): which option best fits “link aggregation for bandwidth/redundancy” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding link aggregation for bandwidth/redundancy helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It removes the value of ECMP for resilience.",
    "It guarantees Wi-Fi channels never overlap.",
    "It implies ARP resolves hostnames directly."
  ]
}
