import type { TemplateConfig } from '../../template-config'

export const networkingTemplate058: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (58/80): which option best fits “path MTU and fragmentation” in the context of broadcast versus collision domains?",
  "variables": {},
  "correct": "Understanding path MTU and fragmentation helps place controls and performance decisions correctly when analyzing broadcast versus collision domains.",
  "distractors": [
    "It implies TCP and UDP are interchangeable for every workload.",
    "It removes the role of ARP on Ethernet segments.",
    "It forbids using VLAN tags on trunks."
  ]
}
