import type { TemplateConfig } from '../../template-config'

export const networkingTemplate034: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (34/80): which option best fits “TCP versus UDP semantics” in the context of latency versus reliability tradeoffs?",
  "variables": {},
  "correct": "Understanding TCP versus UDP semantics helps place controls and performance decisions correctly when analyzing latency versus reliability tradeoffs.",
  "distractors": [
    "It implies TCP and UDP are interchangeable for every workload.",
    "It removes the role of ARP on Ethernet segments.",
    "It forbids using VLAN tags on trunks."
  ]
}
