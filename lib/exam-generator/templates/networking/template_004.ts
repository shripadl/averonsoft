import type { TemplateConfig } from '../../template-config'

export const networkingTemplate004: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (4/80): which option best fits “default gateways and default routes” in the context of failure domains?",
  "variables": {},
  "correct": "Understanding default gateways and default routes helps place controls and performance decisions correctly when analyzing failure domains.",
  "distractors": [
    "It implies firewalls only operate at the physical layer.",
    "It means subnet masks are optional on IPv4 links.",
    "It removes the need for default gateways."
  ]
}
