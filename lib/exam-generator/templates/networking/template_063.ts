import type { TemplateConfig } from '../../template-config'

export const networkingTemplate063: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (63/80): which option best fits “reverse proxies and TLS termination” in the context of broadcast versus collision domains?",
  "variables": {},
  "correct": "Understanding reverse proxies and TLS termination helps place controls and performance decisions correctly when analyzing broadcast versus collision domains.",
  "distractors": [
    "It means routing tables ignore longest-prefix matching.",
    "It eliminates the benefit of keep-alive for long-lived TCP.",
    "It implies proxies cannot terminate TLS sessions."
  ]
}
