import type { TemplateConfig } from '../../template-config'

export const networkingTemplate074: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (74/80): which option best fits “path MTU and fragmentation” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding path MTU and fragmentation helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It implies TCP and UDP are interchangeable for every workload.",
    "It removes the role of ARP on Ethernet segments.",
    "It forbids using VLAN tags on trunks."
  ]
}
