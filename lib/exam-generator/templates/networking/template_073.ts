import type { TemplateConfig } from '../../template-config'

export const networkingTemplate073: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (73/80): which option best fits “ARP on Ethernet segments” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding ARP on Ethernet segments helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It means routers learn hostnames instead of prefixes.",
    "It eliminates the need for MTU discovery.",
    "It requires identical MAC addresses on all hosts."
  ]
}
