import type { TemplateConfig } from '../../template-config'

export const networkingTemplate071: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (71/80): which option best fits “NAT and port translation” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding NAT and port translation helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It means routing tables ignore longest-prefix matching.",
    "It eliminates the benefit of keep-alive for long-lived TCP.",
    "It implies proxies cannot terminate TLS sessions."
  ]
}
