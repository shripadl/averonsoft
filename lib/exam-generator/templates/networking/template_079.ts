import type { TemplateConfig } from '../../template-config'

export const networkingTemplate079: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (79/80): which option best fits “reverse proxies and TLS termination” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding reverse proxies and TLS termination helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It means routing tables ignore longest-prefix matching.",
    "It eliminates the benefit of keep-alive for long-lived TCP.",
    "It implies proxies cannot terminate TLS sessions."
  ]
}
