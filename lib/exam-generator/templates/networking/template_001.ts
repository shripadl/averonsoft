import type { TemplateConfig } from '../../template-config'

export const networkingTemplate001: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (1/80): which option best fits “OSI layering vocabulary” in the context of failure domains?",
  "variables": {},
  "correct": "Understanding OSI layering vocabulary helps place controls and performance decisions correctly when analyzing failure domains.",
  "distractors": [
    "It means routers learn hostnames instead of prefixes.",
    "It eliminates the need for MTU discovery.",
    "It requires identical MAC addresses on all hosts."
  ]
}
