import type { TemplateConfig } from '../../template-config'

export const networkingTemplate056: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (56/80): which option best fits “stateful versus stateless firewalls” in the context of broadcast versus collision domains?",
  "variables": {},
  "correct": "Understanding stateful versus stateless firewalls helps place controls and performance decisions correctly when analyzing broadcast versus collision domains.",
  "distractors": [
    "It guarantees infinite bandwidth without congestion.",
    "It removes the need for any link-layer addressing.",
    "It implies DNS always encrypts application payloads."
  ]
}
