import type { TemplateConfig } from '../../template-config'

export const networkingTemplate069: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (69/80): which option best fits “DNS resolution roles” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding DNS resolution roles helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It guarantees jitter is irrelevant for VoIP quality.",
    "It means ICMP always carries application payloads.",
    "It implies trunks cannot carry more than one VLAN."
  ]
}
