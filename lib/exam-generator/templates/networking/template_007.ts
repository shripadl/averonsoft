import type { TemplateConfig } from '../../template-config'

export const networkingTemplate007: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (7/80): which option best fits “NAT and port translation” in the context of failure domains?",
  "variables": {},
  "correct": "Understanding NAT and port translation helps place controls and performance decisions correctly when analyzing failure domains.",
  "distractors": [
    "It means routing tables ignore longest-prefix matching.",
    "It eliminates the benefit of keep-alive for long-lived TCP.",
    "It implies proxies cannot terminate TLS sessions."
  ]
}
