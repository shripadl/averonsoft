import type { TemplateConfig } from '../../template-config'

export const networkingTemplate075: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (75/80): which option best fits “wireless association and encryption modes (generic)” in the context of security policy insertion points with emphasis on {focus}?",
  "variables": {
    "focus": [
      "runbook tests",
      "dependency drills",
      "scope control"
    ]
  },
  "correct": "Understanding wireless association and encryption modes (generic) helps place controls and performance decisions correctly when analyzing security policy insertion points. (Emphasis: {focus}.)",
  "distractors": [
    "It guarantees wireless links never experience interference.",
    "It means NAT preserves end-to-end transport semantics always.",
    "It removes the value of path MTU discovery."
  ]
}
