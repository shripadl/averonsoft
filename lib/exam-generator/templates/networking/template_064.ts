import type { TemplateConfig } from '../../template-config'

export const networkingTemplate064: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (64/80): which option best fits “SYN flood handling concepts” in the context of broadcast versus collision domains?",
  "variables": {},
  "correct": "Understanding SYN flood handling concepts helps place controls and performance decisions correctly when analyzing broadcast versus collision domains.",
  "distractors": [
    "It guarantees infinite bandwidth without congestion.",
    "It removes the need for any link-layer addressing.",
    "It implies DNS always encrypts application payloads."
  ]
}
