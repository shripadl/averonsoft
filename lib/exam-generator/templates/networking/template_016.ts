import type { TemplateConfig } from '../../template-config'

export const networkingTemplate016: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (16/80): which option best fits “SYN flood handling concepts” in the context of failure domains?",
  "variables": {},
  "correct": "Understanding SYN flood handling concepts helps place controls and performance decisions correctly when analyzing failure domains.",
  "distractors": [
    "It guarantees infinite bandwidth without congestion.",
    "It removes the need for any link-layer addressing.",
    "It implies DNS always encrypts application payloads."
  ]
}
