import type { TemplateConfig } from '../../template-config'

export const networkingTemplate072: TemplateConfig = {
  "domain": "networking",
  "stem": "Networking study item (72/80): which option best fits “stateful versus stateless firewalls” in the context of security policy insertion points?",
  "variables": {},
  "correct": "Understanding stateful versus stateless firewalls helps place controls and performance decisions correctly when analyzing security policy insertion points.",
  "distractors": [
    "It guarantees infinite bandwidth without congestion.",
    "It removes the need for any link-layer addressing.",
    "It implies DNS always encrypts application payloads."
  ]
}
