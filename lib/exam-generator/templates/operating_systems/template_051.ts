import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate051: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (51/80): which statement best matches CPU scheduling goals when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties CPU scheduling goals to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every process has identical virtual mappings.",
    "It eliminates the role of inode metadata.",
    "It implies swap space is unrelated to performance."
  ]
}
