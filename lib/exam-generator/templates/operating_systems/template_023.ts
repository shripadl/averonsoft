import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate023: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (23/80): which statement best matches deadlock prerequisites when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties deadlock prerequisites to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every syscall is O(1) regardless of work done.",
    "It removes the value of file locking for coordination.",
    "It implies copy-on-write never saves memory."
  ]
}
