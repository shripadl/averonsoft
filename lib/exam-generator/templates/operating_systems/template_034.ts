import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate034: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (34/80): which statement best matches virtual memory and paging when prioritizing reliability?",
  "variables": {},
  "correct": "Kernel design ties virtual memory and paging to measurable reliability characteristics for workloads and policies.",
  "distractors": [
    "It means deadlocks cannot occur with a single mutex.",
    "It removes the benefit of copy-on-write after fork.",
    "It implies the kernel never mediates hardware access."
  ]
}
