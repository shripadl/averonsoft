import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate056: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (56/80): which statement best matches copy-on-write after fork when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties copy-on-write after fork to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It means threads never share an address space.",
    "It removes the distinction between kernel and user mode.",
    "It implies virtual memory cannot overcommit physical RAM."
  ]
}
