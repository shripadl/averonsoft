import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate072: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (72/80): which statement best matches copy-on-write after fork when prioritizing capacity planning signals?",
  "variables": {},
  "correct": "Kernel design ties copy-on-write after fork to measurable capacity planning signals characteristics for workloads and policies.",
  "distractors": [
    "It means threads never share an address space.",
    "It removes the distinction between kernel and user mode.",
    "It implies virtual memory cannot overcommit physical RAM."
  ]
}
