import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate031: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (31/80): which statement best matches memory-mapped files (introductory) when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties memory-mapped files (introductory) to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every syscall is O(1) regardless of work done.",
    "It removes the value of file locking for coordination.",
    "It implies copy-on-write never saves memory."
  ]
}
