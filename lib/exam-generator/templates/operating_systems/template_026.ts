import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate026: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (26/80): which statement best matches thrashing and memory pressure when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties thrashing and memory pressure to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It means deadlocks cannot occur with a single mutex.",
    "It removes the benefit of copy-on-write after fork.",
    "It implies the kernel never mediates hardware access."
  ]
}
