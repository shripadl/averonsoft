import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate059: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (59/80): which statement best matches real-time priority inversion (introductory) when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties real-time priority inversion (introductory) to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every process has identical virtual mappings.",
    "It eliminates the role of inode metadata.",
    "It implies swap space is unrelated to performance."
  ]
}
