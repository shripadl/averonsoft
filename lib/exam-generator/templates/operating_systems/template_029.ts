import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate029: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (29/80): which statement best matches signal delivery (introductory) when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties signal delivery (introductory) to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
