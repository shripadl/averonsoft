import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate045: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (45/80): which statement best matches signal delivery (introductory) when prioritizing reliability with emphasis on {focus}?",
  "variables": {
    "focus": [
      "runbook tests",
      "dependency drills",
      "scope control"
    ]
  },
  "correct": "Kernel design ties signal delivery (introductory) to measurable reliability characteristics for workloads and policies. (Emphasis: {focus}.)",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
