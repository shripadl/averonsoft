import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate011: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (11/80): which statement best matches real-time priority inversion (introductory) when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties real-time priority inversion (introductory) to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every process has identical virtual mappings.",
    "It eliminates the role of inode metadata.",
    "It implies swap space is unrelated to performance."
  ]
}
