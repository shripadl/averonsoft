import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate008: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (8/80): which statement best matches copy-on-write after fork when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties copy-on-write after fork to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It means threads never share an address space.",
    "It removes the distinction between kernel and user mode.",
    "It implies virtual memory cannot overcommit physical RAM."
  ]
}
