import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate064: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (64/80): which statement best matches hardware abstraction and drivers (introductory) when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties hardware abstraction and drivers (introductory) to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It means threads never share an address space.",
    "It removes the distinction between kernel and user mode.",
    "It implies virtual memory cannot overcommit physical RAM."
  ]
}
