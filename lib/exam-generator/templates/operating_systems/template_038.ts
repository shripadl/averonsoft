import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate038: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (38/80): which statement best matches file descriptors and inode metadata (introductory) when prioritizing reliability?",
  "variables": {},
  "correct": "Kernel design ties file descriptors and inode metadata (introductory) to measurable reliability characteristics for workloads and policies.",
  "distractors": [
    "It means user-mode drivers are always preferred for disks.",
    "It eliminates the role of TLB shootdowns in performance.",
    "It implies scheduling is unrelated to cache locality."
  ]
}
