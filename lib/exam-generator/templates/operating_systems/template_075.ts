import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate075: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (75/80): which statement best matches real-time priority inversion (introductory) when prioritizing capacity planning signals with emphasis on {focus}?",
  "variables": {
    "focus": [
      "runbook tests",
      "dependency drills",
      "scope control"
    ]
  },
  "correct": "Kernel design ties real-time priority inversion (introductory) to measurable capacity planning signals characteristics for workloads and policies. (Emphasis: {focus}.)",
  "distractors": [
    "It guarantees every process has identical virtual mappings.",
    "It eliminates the role of inode metadata.",
    "It implies swap space is unrelated to performance."
  ]
}
