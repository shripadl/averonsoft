import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate070: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (70/80): which statement best matches file descriptors and inode metadata (introductory) when prioritizing capacity planning signals with emphasis on {focus}?",
  "variables": {
    "focus": [
      "policy attestation",
      "training records",
      "control testing"
    ]
  },
  "correct": "Kernel design ties file descriptors and inode metadata (introductory) to measurable capacity planning signals characteristics for workloads and policies. (Emphasis: {focus}.)",
  "distractors": [
    "It means user-mode drivers are always preferred for disks.",
    "It eliminates the role of TLB shootdowns in performance.",
    "It implies scheduling is unrelated to cache locality."
  ]
}
