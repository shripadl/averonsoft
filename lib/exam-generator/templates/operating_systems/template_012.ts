import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate012: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (12/80): which statement best matches namespaces and isolation (generic) when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties namespaces and isolation (generic) to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It means real-time tasks never need priority inversion handling.",
    "It removes the value of journaling file systems.",
    "It implies threads and processes are identical terms."
  ]
}
