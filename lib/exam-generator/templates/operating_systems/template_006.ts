import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate006: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (6/80): which statement best matches file descriptors and inode metadata (introductory) when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties file descriptors and inode metadata (introductory) to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It means user-mode drivers are always preferred for disks.",
    "It eliminates the role of TLB shootdowns in performance.",
    "It implies scheduling is unrelated to cache locality."
  ]
}
