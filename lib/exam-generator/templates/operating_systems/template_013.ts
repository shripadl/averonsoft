import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate013: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (13/80): which statement best matches signal delivery (introductory) when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties signal delivery (introductory) to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
