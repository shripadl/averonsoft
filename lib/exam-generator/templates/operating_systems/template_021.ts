import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate021: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (21/80): which statement best matches system calls as controlled gates when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties system calls as controlled gates to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
