import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate061: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (61/80): which statement best matches signal delivery (introductory) when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties signal delivery (introductory) to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
