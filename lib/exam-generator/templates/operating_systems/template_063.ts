import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate063: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (63/80): which statement best matches memory-mapped files (introductory) when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties memory-mapped files (introductory) to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every syscall is O(1) regardless of work done.",
    "It removes the value of file locking for coordination.",
    "It implies copy-on-write never saves memory."
  ]
}
