import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate007: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (7/80): which statement best matches deadlock prerequisites when prioritizing security?",
  "variables": {},
  "correct": "Kernel design ties deadlock prerequisites to measurable security characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every syscall is O(1) regardless of work done.",
    "It removes the value of file locking for coordination.",
    "It implies copy-on-write never saves memory."
  ]
}
