import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate027: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (27/80): which statement best matches real-time priority inversion (introductory) when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties real-time priority inversion (introductory) to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It guarantees every process has identical virtual mappings.",
    "It eliminates the role of inode metadata.",
    "It implies swap space is unrelated to performance."
  ]
}
