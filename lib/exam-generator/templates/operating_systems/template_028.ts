import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate028: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (28/80): which statement best matches namespaces and isolation (generic) when prioritizing performance?",
  "variables": {},
  "correct": "Kernel design ties namespaces and isolation (generic) to measurable performance characteristics for workloads and policies.",
  "distractors": [
    "It means real-time tasks never need priority inversion handling.",
    "It removes the value of journaling file systems.",
    "It implies threads and processes are identical terms."
  ]
}
