import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate053: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (53/80): which statement best matches system calls as controlled gates when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties system calls as controlled gates to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
