import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate073: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (73/80): which statement best matches page cache and read performance when prioritizing capacity planning signals?",
  "variables": {},
  "correct": "Kernel design ties page cache and read performance to measurable capacity planning signals characteristics for workloads and policies.",
  "distractors": [
    "It guarantees scheduling fairness without any priorities.",
    "It eliminates the need for file permissions.",
    "It implies paging never causes page faults."
  ]
}
