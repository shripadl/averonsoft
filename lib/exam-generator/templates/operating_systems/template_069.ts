import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate069: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (69/80): which statement best matches system calls as controlled gates when prioritizing capacity planning signals?",
  "variables": {},
  "correct": "Kernel design ties system calls as controlled gates to measurable capacity planning signals characteristics for workloads and policies.",
  "distractors": [
    "It guarantees swap eliminates the need for RAM sizing.",
    "It removes the benefit of cgroup limits for fairness.",
    "It implies page faults are always errors to ignore."
  ]
}
