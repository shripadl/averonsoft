import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate048: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (48/80): which statement best matches hardware abstraction and drivers (introductory) when prioritizing reliability?",
  "variables": {},
  "correct": "Kernel design ties hardware abstraction and drivers (introductory) to measurable reliability characteristics for workloads and policies.",
  "distractors": [
    "It means threads never share an address space.",
    "It removes the distinction between kernel and user mode.",
    "It implies virtual memory cannot overcommit physical RAM."
  ]
}
