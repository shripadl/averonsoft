import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate054: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (54/80): which statement best matches file descriptors and inode metadata (introductory) when prioritizing debuggability?",
  "variables": {},
  "correct": "Kernel design ties file descriptors and inode metadata (introductory) to measurable debuggability characteristics for workloads and policies.",
  "distractors": [
    "It means user-mode drivers are always preferred for disks.",
    "It eliminates the role of TLB shootdowns in performance.",
    "It implies scheduling is unrelated to cache locality."
  ]
}
