import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate041: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (41/80): which statement best matches page cache and read performance when prioritizing reliability?",
  "variables": {},
  "correct": "Kernel design ties page cache and read performance to measurable reliability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees scheduling fairness without any priorities.",
    "It eliminates the need for file permissions.",
    "It implies paging never causes page faults."
  ]
}
