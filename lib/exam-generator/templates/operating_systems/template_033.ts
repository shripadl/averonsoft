import type { TemplateConfig } from '../../template-config'

export const operatingsystemsTemplate033: TemplateConfig = {
  "domain": "operating_systems",
  "stem": "Operating systems (33/80): which statement best matches process versus thread models when prioritizing reliability?",
  "variables": {},
  "correct": "Kernel design ties process versus thread models to measurable reliability characteristics for workloads and policies.",
  "distractors": [
    "It guarantees scheduling fairness without any priorities.",
    "It eliminates the need for file permissions.",
    "It implies paging never causes page faults."
  ]
}
